```markdown
# Python Practice Questions

### Section 1: Basic Output and Comments

1.  What is the output of the following Python code?
    ```python
    print("Welcome to Python!")
    ```

2.  How do you add a single-line comment in Python? Provide an example.

3.  Demonstrate how to write a multi-line comment using triple double-quotes.

### Section 2: Variables, Data Types, and Operators

4.  Identify the data type for each of the following values: `50`, `7.8`, `"Coding"`.

5.  If `greeting = "Hello"`, what will `print(greeting)` output?

6.  Given `x = 15` and `y = 4`, calculate the results of `x // y` (floor division) and `x % y` (modulus).

7.  What will be the output of `print("AI" * 3)`?

8.  Explain the difference in behavior for the `+` operator in `print("10" + "5")` versus `print(10 + 5)`.

### Section 3: User Input and Type Casting

9.  Write Python code to ask the user for their favorite animal and store it in a variable called `animal`.

10. Write Python code to take two integer numbers from the user and print their sum. Ensure proper type conversion.

11. Why is it necessary to use `int()` or `float()` when taking numerical input from the user using the `input()` function?

### Section 4: String Formatting

12. Given `product = "Laptop"` and `price = 1200`, use an f-string to print: "The price of the Laptop is $1200."

13. If `num1 = 7` and `num2 = 3`, use `print()` with comma separation to output: "The numbers are 7 and 3."

### Section 5: Conditional Statements

14. Write an `if-else` statement that checks if a `temperature` variable (e.g., `28`) is greater than `25`. If it is, print "It's warm!"; otherwise, print "It's cool."

15. Consider the notebook's `if-elif-else` example that checks if a number is positive, negative, or zero. If the user enters `-5`, what will be the output?
```